# AGENTS.md

Project documentation is consolidated under `md files/`.
Read `md files/CLAUDE.md` first, followed by `md files/PLAN.md`.
This root pointer must remain here so coding tools can discover the repository rules automatically.

If the local `md files/` documentation is absent in a fresh checkout, read `README.md` for the tracked project structure, development commands, and verification workflow. Preserve the existing design and animation behavior during maintenance.
